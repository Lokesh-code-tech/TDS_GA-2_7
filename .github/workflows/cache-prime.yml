name: Cache Prime Minimal

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Restore dummy cache
        id: cache-deps
        uses: actions/cache@v4
        with:
          path: ~/.dummy-cache
          key: cache-03e1cf5

      - name: prime-cache-03e1cf5
        run: echo "Cache hit? ${{ steps.cache-deps.outputs.cache-hit }}"
